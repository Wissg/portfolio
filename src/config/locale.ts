// A list of all locales that are supported
export const locales: Array<string> = ['en', 'fr'];

// Default locale when no locale matches
export const defaultLocale: string = 'fr';

export const localeCookieKey: string = "locale"
